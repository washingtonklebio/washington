# Ver chave ssh da máquina 
cat /root/.ssh/id_rsa.pub

# Listar links simbolicos
ls -l /usr/bin/python

# Alterar links simbolicos
ln -sf /usr/bin/python3.6 /usr/bin/python

# Instalar Xvfb
dnf -y install xorg-x11-server-Xvfb

# Recarregar configurações do httpd
systemctl reload httpd

# Agendamentos crontab
cat /etc/crontab

# Listar todos os processos php
ps aux | grep php

# Matar um processo 
kill -9 "id_processo"

# Ver processamento da máquina 
top

# Descomparctar arquivos tar
tar -vzxf nome_arquivo.tar.gz

